function calcularDesconto (preco: number, percentual: number): number {
    let valorFinal: number = (preco - (let valorFinal: number 00));
    return valorFinal;
}
console.log(calcularDesconto(100,10));